import { DetailPipePipe } from './detail-pipe.pipe';

describe('DetailPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DetailPipePipe();
    expect(pipe).toBeTruthy();
  });
});
